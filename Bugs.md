# Bugs & Issues

*Known bugs, risks, and issues spotted while thinking or building. Newest at top. Tick items off as they're resolved; note the build that fixed them.*

**Legend:** 🔴 blocker · 🟠 important · 🟡 nice-to-fix · 🔵 risk/watchlist (not yet a bug)

---

## Open

- [ ] 🟠 **Claiming misses the add funnel, so pre-account practitioners create duplicates (Josh, July 11).** Two compounding gaps. (1) The "Is this you? Claim it" card lives only on `/my-practice`, but the recruitment funnel sends everyone through `/add-practitioner`, which never checks for a claimable listing: a signed-in practitioner with no owned listing gets the blank form and recreates their practice (Josh: unowned `josh-hewett-2` from July 3 + new owned `josh-hewett` tonight, both live, the old one invisible to him because My practice lists owned rows only). (2) Claim-by-email compares the **sign-in email to the listing's contact email**; Josh's account email differs from his listing's contact address, so even `/my-practice` would have offered nothing (the manage-link claim is the fallback for that). Proposed fix: a claim intercept on `/add-practitioner` (claim card before the blank form, with a "this is a different practice" escape), plus a duplicate backstop in `submitPractitioner` (same practice name or contact email as an existing listing ⇒ hold `pending` + "possible duplicate" steward alert, never two live twins — fits the notify-a-human moderation model).
- [ ] 🟠 **Public-write spam/bot flood.** The public write paths (`/add-practitioner`, `/report`, `/feedback`) run a content-check on submissions but have **no rate-limiting or bot check yet** — add a honeypot field + per-IP throttle before wide public launch.
- [ ] 🔵 **Avatar upload is an unauthenticated write path.** `uploadAvatar` (service-role) is callable by anyone (no login) — it validates image type + caps size at 2 MB, but a script could still fill the `avatars` bucket. Fine for the small pilot (free tier is 1 GB); add rate-limiting / a bot check + periodic orphan-file cleanup before wide launch. *(Build 24.)*
- [ ] 🔵 **Resend free-tier caps (100 emails/day, 3,000/month).** Everything rides one account: Supabase auth emails (via SMTP), steward alerts (each recipient counts), recommendation notifications. Google sign-ins cost zero. Watch the Resend dashboard on launch-announcement day; Pro ($20/mo, 50k) is a no-code-change upgrade. Also bump Supabase Auth's own "emails per hour" rate limit (~100/hr) before launch — it throttles first.
- [ ] 🟡 **`middleware.ts` → `proxy.ts` deprecation.** Next.js warns on every build that the `middleware` file convention is deprecated in favour of `proxy`. Rename `src/middleware.ts` (and its `config` matcher) before a future Next major; cosmetic today.
- [x] 🟡 **Recurring-event horizon.** *(Build 9)* The daily Vercel Cron import (`/api/cron/import`) re-runs automatically, keeping the 120-day recurrence window fresh.
- [x] 🟡 **Cron-imported events aren't geocoded.** *(Build 10)* The daily cron now runs `geocodePending` after the import — geocoding new addressed events + practitioners (capped 20/run, throttled ~1/sec), so they join "near me" automatically.
- [ ] 🔵 **Stored XSS in user text.** Descriptions/bios/event text are user-supplied; keep them escaped on render (React does this by default) and never inject via `dangerouslySetInnerHTML`. Imported HTML is stripped at import time.
- [ ] 🔵 **Service-role key leakage.** Must never reach the client bundle; it's confined to server actions/scripts (`server-only` guard on `src/lib/supabase/admin.ts`). The key was shared in chat during setup — rotate it before wide launch.
- [ ] 🔵 **Sybil limits of no-login flagging.** Accepted for v1 (dedupe + human-in-loop + no auto-action). Revisit if abuse escalates; v2 accounts raise the bar.

---

## Resolved

- [x] 🟠 **Category admin misbehaved three ways (reported July 14).** *(Build 93)* (1) **Adding "stopped working":** `createCategory` swallowed insert errors, and a *deactivated* category still holds its unique slug — so deactivate-then-re-add-something-similar failed silently and read as "categories are capped" (there is no cap; 21 exist). Now every category action lands with a visible outcome banner (`?notice=`), an active same-slug twin explains itself, and a deactivated twin is **reactivated under the new name**. (2) **Renames looked unsaved:** they saved fine, but the `CategoryRail` hardcodes short labels for seeded slugs; now each `CATEGORY_META` entry carries its `seedName` and the curated label applies only while the seeded name stands — renamed categories derive their label from the current name. (3) **No delete + insertion ordering:** added `deleteCategory` (only when no practices/events use it; otherwise a banner says to deactivate), and categories are now **alphabetical** in the app (`getCategories`) and the admin list (`listCategoriesAdmin`); `sort_order` remains in the schema but no longer drives display.

- [x] 🔴 **Password-reset email links always failed ("sign-in could not be completed").** *(Build 77)* The links used Supabase's PKCE `ConfirmationURL` flow, which silently requires the same browser that requested the email — reset emails are precisely the emails opened elsewhere. Fixed with the `token_hash` + server-side `verifyOtp` pattern (`/auth/confirm`); both email templates re-issued.
- [x] 🟠 **Search couldn't find practitioners by category, service, or bio.** *(Build 74, migration `0010`)* The search vector was a generated column, which can't see other tables. Replaced with a trigger-maintained weighted vector (name/practice → categories+keywords → description+services → bio/area/languages) + prefix matching ("mass" finds massage).
- [x] 🔵 **Steward alerts reached only one inbox.** *(July 9, config)* Resolved by verifying `myhearthapp.ca` in Resend + `RESEND_FROM` — alerts now deliver to any `NOTIFY_EMAILS` recipient from the Hearth domain.
- [x] 🔴 **myhearthapp.ca went dark (July 10 DNS outage).** Nameservers had been switched to Vercel, abandoning the Porkbun zone (site + email records) with no Vercel zone serving. Reverted to Porkbun's default nameservers; guide rewritten with a never-switch warning (`Domain Setup.md` Part 1).
- [x] 🟠 **OAuth sign-in landed on the homepage instead of returning to the flow.** *(Build 66–67 + config)* The canonical host is **www**; the Supabase redirect allowlist only had the apex, so Supabase fell back to the Site URL. Fixed by adding `https://www.myhearthapp.ca/**` + Site URL to www; code hardening added (cookie-stashed `next`, header sign-in carries the current page).
- [x] 🔴 **Hidden practitioners still showed on the public directory for a logged-in admin.** *(Build 43)* Hiding set `status = 'hidden'` correctly (button flipped to Publish), but the listing stayed visible on `/practitioners` when viewed **while signed into the admin panel**. Root cause: `getPractitioners` (and `getEvents`) relied **only on RLS** to filter status, with no explicit `.eq("status","live")`. RLS is viewer-dependent — a logged-in admin's session cookie rides along on the anon-key server client, making them `authenticated`, and the `practitioners_admin_all` policy (`for all to authenticated using (true)`) grants them **every** status. So an admin (but not a real visitor) saw hidden/pending rows in the public list. Verified with the anon key (RLS correctly hides them from true anon) and reproduced the admin view with the service role. Fix: filter `status = 'live'` **explicitly** in both public reads (matching `getPractitionerBySlug`/`getEventsByHost`, which already did) — so the public list is correct regardless of who's viewing; RLS stays as the anon backstop. The Reports-tab "Hide listing" button was never broken (it calls the same working action); it looked inert for the same reason, and because the report card stays in the inbox until "Dismiss reports" (by design).
- [x] 🟠 **Admin "Hide" looked broken (no feedback on the listings screen).** *(Build 40)* Reported as "Hide does nothing, but Delete works." The hide **was** working — a hidden listing is removed from the public directory (public reads are live-only) — but on `/admin/listings` the row *stays* (so you can restore it) and only a tiny "LIVE → HIDDEN" text label changed, so it looked like nothing happened (vs Delete, which removes the row). Verified the service-role UPDATE persists by reproducing it directly against the DB. Fix: coloured status badge + dimmed hidden rows + a "N live · N hidden … don't show in the public directory" summary line; and `setPractitionerStatus`/`setEventStatus` now surface DB errors instead of swallowing them.
- [x] 🟠 **Category picker let you tick more than 3, then blocked the submit.** *(Build 39)* The "up to 3" cap lived only in the server actions, so the form happily accepted more selections and then failed on submit (bit at least two testers). Per the July 6 call decision the cap is **removed entirely**: both `submitPractitioner` and `updateListing` now require only ≥ 1 category, and the form hint reads "choose all that apply."
- [x] 🟠 **Instagram button went to a broken link.** *(Build 26)* A practitioner usually enters a handle (`@sarah` / `sarah`), and the contact button used `externalHref`, producing `https://sarah` (a dead link). Added `instagramUrl()` — normalizes a handle, `instagram.com/handle`, or a full URL into `https://instagram.com/<handle>`; used on the card + profile.
- [x] 🟠 **Instagram-only listings were blocked.** *(Build 17)* The at-least-one-contact rule (app validation + DB `CHECK`) counted WhatsApp/Email/Website but **not** Instagram, while the add-practitioner form grouped all four as "at least one required" — so an Instagram-only submit failed. Instagram now counts, in both the validation and the constraint (migration `0003_instagram_contact.sql`).
- [x] 🟠 **Practitioners could vanish from "near me".** *(Build 16)* `area` was optional, so a location-less (or ungeocodable) listing never got coordinates and dropped out of distance search. `area` is now required and entered via the type-ahead `AddressAutocomplete`, which pins area-level coords on submit.
- [x] 🟡 **No way to grab a shareable profile link.** *(Build 15)* Profiles were shareable URLs but had no copy affordance, and the "you're live" screen didn't reveal the link. Added a `ShareButton` (native share / copy) on the profile and the success screen.
- [x] 🟠 **"Near me" showed one recurring event repeated.** *(Build 8)* Imported recurring events are stored as one row per occurrence, so the two addressed weekly yoga series (14 + 15 rows) dominated the distance-sorted list. Fixed by collapsing each series to its next upcoming occurrence in `getEvents` (`collapseSeries`, keyed by the `external_id` UID prefix). Also declutters the time agenda.
- [x] 🟠 **Raw `href`/HTML in imported event descriptions.** *(Build 6)* Import strips HTML, decodes entities, extracts the link from `<a href>`.
- [x] 🔵 **RLS before any public write path.** *(Build 3)* `0001_initial_schema.sql`: anon read-only & live-only; admins full; public writes via service role.
- [x] 🔵 **Server-controlled status.** *(Build 4)* `status`/`auto_check` set in the service-role server actions from the content-check; never from the client.
- [x] 🔵 **Google Calendar API key scope.** *(Build 5)* N/A — import reads the public iCal feed, no key.
- [x] 🔵 **Seed-import dedupe.** *(Build 5)* Skips existing `external_id`s (a second run inserts 0).
